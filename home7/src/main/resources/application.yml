spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

  liquibase:
    change-log: classpath:db/changelog.xml

  shell:
    interactive:
      enabled: false

logging:
  level:
    ROOT: info

