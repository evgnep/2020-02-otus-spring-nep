spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

  liquibase:
    change-log: classpath:db/changelog.xml

logging:
  level:
    ROOT: DEBUG

